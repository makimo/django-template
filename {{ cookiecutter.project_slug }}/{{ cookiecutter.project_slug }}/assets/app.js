// App code-base initialization goes here.

// Load app-wide styles. Those will affect
// every component. For Vue-component-specific styles
// see scoped CSS.
import './app.scss';

// Vendor-specific scripts and initialization
import './vendor/example.js';

// Shared utilities (e.g. CSRF token injector)
import './scripts/shared.js';
