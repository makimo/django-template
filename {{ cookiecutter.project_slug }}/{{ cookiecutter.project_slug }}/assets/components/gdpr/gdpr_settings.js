/** Const options for gdpr module. */

/** Value for cookie when permission was accepted. */
const acceptValue = 'accepted';

/** Value for cookie when permission was rejected. */
const falseValue = 'false';

/** Name of main cookie for gdpr module. */
const cookieName = 'gdpr';

/** Cookies expire time. */
const expired_at = 1000;

export { acceptValue, falseValue, cookieName, expired_at }
